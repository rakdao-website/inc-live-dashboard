export function isOpenAIResponsesCompactionAwareSession(session) {
    return (!!session &&
        typeof session.runCompaction ===
            'function');
}
export function isRunContextAwareSession(session) {
    return (!!session &&
        session.acceptsRunContext ===
            true);
}
export function isSessionHistoryRewriteAwareSession(session) {
    return (!!session &&
        typeof session
            .applyHistoryMutations === 'function');
}
export function isSessionHistoryTransactionAwareSession(session) {
    return (!!session &&
        typeof session
            .applyHistoryTransaction === 'function');
}
//# sourceMappingURL=session.mjs.map