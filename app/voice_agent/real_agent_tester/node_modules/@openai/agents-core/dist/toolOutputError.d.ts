import { InvalidToolOutputError } from './errors';
export type InvalidToolOutputFailure = {
    error: InvalidToolOutputError;
    redacted: boolean;
    disposition: InvalidToolOutputDisposition;
};
export type InvalidToolOutputDisposition = {
    redacted: boolean;
};
