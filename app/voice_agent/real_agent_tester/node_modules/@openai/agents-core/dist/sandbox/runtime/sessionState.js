"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSerializedSandboxState = getSerializedSandboxState;
exports.sanitizeSerializedSandboxState = sanitizeSerializedSandboxState;
exports.toSessionStateEnvelope = toSessionStateEnvelope;
exports.assertSessionStateEnvelope = assertSessionStateEnvelope;
exports.deserializeSandboxSessionStateEntry = deserializeSandboxSessionStateEntry;
exports.resolveTrustedManifestForResume = resolveTrustedManifestForResume;
exports.assertTrustedManifestForDockerRunState = assertTrustedManifestForDockerRunState;
exports.getSerializedSessionEntryForAgent = getSerializedSessionEntryForAgent;
exports.serializedSessionEntryHasInContainerMounts = serializedSessionEntryHasInContainerMounts;
exports.serializedSessionEntryRequiresFreshMountAuthority = serializedSessionEntryRequiresFreshMountAuthority;
exports.getPreviousSerializedSessionsByAgent = getPreviousSerializedSessionsByAgent;
exports.hasPreservedOwnedSessions = hasPreservedOwnedSessions;
const errors_1 = require("../../errors.js");
const errors_2 = require("../errors.js");
const manifest_1 = require("../manifest.js");
const session_1 = require("../session.js");
const manifestPersistence_1 = require("../sandboxes/shared/manifestPersistence.js");
const mountSecurity_1 = require("../mountSecurity.js");
const sessionStateTrust_1 = require("../internal/sessionStateTrust.js");
function getSerializedSandboxState(runState) {
    return runState?._sandbox;
}
function sanitizeSerializedSandboxState(state) {
    const sessionsByAgent = Object.fromEntries(Object.entries(state.sessionsByAgent).map(([agentKey, entry]) => [
        agentKey,
        {
            ...entry,
            sessionState: sanitizeSerializedSessionEnvelope(entry.sessionState),
        },
    ]));
    return {
        ...state,
        sessionState: sanitizeSerializedSessionEnvelope(state.sessionState),
        sessionsByAgent,
    };
}
function sanitizeSerializedSessionEnvelope(envelope) {
    const hasNonResumableMountAuthority = (0, mountSecurity_1.persistedManifestRecordHasNonResumableMountAuthority)(envelope.manifest);
    const existingMetadata = (0, manifestPersistence_1.deserializeMountCredentialRedactionMetadata)(envelope.providerState);
    const existingPaths = existingMetadata[mountSecurity_1.REDACTED_MOUNT_CREDENTIAL_PATHS_KEY] ?? [];
    const { record, credentialPaths } = (0, mountSecurity_1.sanitizePersistedManifestRecord)(envelope.manifest);
    const persistedManifest = (0, manifestPersistence_1.deserializeManifest)(record);
    const redactedPaths = [...new Set([...existingPaths, ...credentialPaths])];
    (0, mountSecurity_1.validateMountCredentialRedactionPaths)(persistedManifest, redactedPaths);
    const serializedSanitizedManifest = (0, manifestPersistence_1.serializeManifestRecord)((0, mountSecurity_1.sanitizeMountCredentialEnvironmentForPersistence)({
        manifest: persistedManifest,
    }).manifest);
    const persistentManifest = {
        ...record,
        entries: serializedSanitizedManifest.entries,
        environment: serializedSanitizedManifest.environment,
    };
    const providerState = providerStateWithoutSdkEnvelopeFields(envelope.providerState, envelope.backendId);
    const persistedEnvironment = isStringRecord(providerState.environment)
        ? providerState.environment
        : undefined;
    delete providerState.environment;
    if (persistedEnvironment) {
        providerState.environment =
            (0, mountSecurity_1.sanitizeMountCredentialEnvironmentForPersistence)({
                manifest: persistedManifest,
                environment: persistedEnvironment,
            }).environment;
    }
    if (redactedPaths.length > 0) {
        providerState[mountSecurity_1.REDACTED_MOUNT_CREDENTIAL_PATHS_KEY] = redactedPaths;
    }
    else {
        delete providerState[mountSecurity_1.REDACTED_MOUNT_CREDENTIAL_PATHS_KEY];
    }
    if (hasNonResumableMountAuthority) {
        providerState[mountSecurity_1.NON_RESUMABLE_MOUNT_AUTHORITY_KEY] = true;
    }
    return {
        ...envelope,
        manifest: persistentManifest,
        providerState,
    };
}
function toSessionStateEnvelope(backendId, state, providerState) {
    (0, mountSecurity_1.validateMountEnvironmentCredentialBoundaries)(state.manifest, state.environment ?? {});
    const persistentManifest = (0, manifestPersistence_1.serializeManifest)((0, mountSecurity_1.sanitizeMountCredentialEnvironmentForPersistence)(state).manifest);
    return {
        version: session_1.SANDBOX_SESSION_STATE_VERSION,
        backendId,
        manifest: (0, manifestPersistence_1.serializeManifestRecord)(persistentManifest),
        ...(state.snapshot !== undefined ? { snapshot: state.snapshot } : {}),
        ...(state.snapshotFingerprint !== undefined
            ? { snapshotFingerprint: state.snapshotFingerprint }
            : {}),
        ...(state.snapshotFingerprintVersion !== undefined
            ? { snapshotFingerprintVersion: state.snapshotFingerprintVersion }
            : {}),
        workspaceReady: state.workspaceReady ?? true,
        ...(state.exposedPorts
            ? { exposedPorts: structuredClone(state.exposedPorts) }
            : {}),
        // Keep provider-owned fields separate from the SDK envelope so version and backend
        // checks can run before deserializing provider-specific state.
        providerState: {
            ...providerStateWithoutSdkEnvelopeFields(providerState, backendId),
            ...(0, manifestPersistence_1.serializeHostPathGrantRedactionMetadata)(state),
            ...(0, manifestPersistence_1.serializeMountCredentialRedactionMetadata)(state),
        },
    };
}
function providerStateWithoutSdkEnvelopeFields(providerState, backendId) {
    const sanitized = { ...providerState };
    delete sanitized.manifest;
    if (backendId === 'docker') {
        delete sanitized.sessionIdentity;
    }
    delete sanitized.snapshot;
    delete sanitized.snapshotFingerprint;
    delete sanitized.snapshotFingerprintVersion;
    delete sanitized.workspaceReady;
    delete sanitized.exposedPorts;
    return sanitized;
}
function assertSessionStateEnvelope(client, envelope) {
    if (!session_1.SUPPORTED_SANDBOX_SESSION_STATE_VERSIONS.includes(envelope.version)) {
        throw new errors_1.UserError(`Sandbox session state version ${envelope.version} is not supported. Please use version ${session_1.SANDBOX_SESSION_STATE_VERSION}.`);
    }
    if (envelope.backendId !== client.backendId) {
        throw new errors_1.UserError('RunState sandbox session backend does not match the configured sandbox client.');
    }
}
async function deserializeSandboxSessionStateEntry(client, serializedEntry, trustedManifest, trustedConfig = {}) {
    if (!serializedEntry) {
        return undefined;
    }
    if (serializedEntry.backendId !== client.backendId) {
        throw new errors_1.UserError('RunState sandbox backend does not match the configured sandbox client.');
    }
    const resolvedTrustedManifest = resolveTrustedManifestForResume(client, trustedManifest, trustedConfig.clientOptions);
    assertTrustedManifestForDockerRunState(client, resolvedTrustedManifest);
    if (!client.deserializeSessionState) {
        throw new errors_1.UserError('Sandbox client must implement deserializeSessionState() to resume RunState sandbox state.');
    }
    const envelope = serializedEntry.sessionState;
    assertSessionStateEnvelope(client, envelope);
    assertTrustedEnvironmentForPersistedReferences(envelope, resolvedTrustedManifest);
    if ((0, mountSecurity_1.manifestHasInContainerMounts)((0, manifestPersistence_1.deserializeManifest)(envelope.manifest))) {
        throw new errors_2.SandboxMountError('Sandbox session state with in-container mounts cannot be resumed safely. Create a fresh sandbox so mount helpers are recreated from current trusted configuration.', undefined, 'mount_config_invalid');
    }
    const prevalidatedPersistedState = (0, manifestPersistence_1.rebindPersistedMountCredentials)({
        manifest: (0, manifestPersistence_1.deserializeManifest)(envelope.manifest),
        ...(0, manifestPersistence_1.deserializeMountCredentialRedactionMetadata)(envelope.providerState),
    }, resolvedTrustedManifest);
    (0, manifestPersistence_1.assertMountCredentialsRebound)(prevalidatedPersistedState);
    client.validateSessionStateForResume?.({
        source: 'runState',
        state: providerStateWithSdkEnvelopeFields(envelope, prevalidatedPersistedState.manifest, resolvedTrustedManifest),
    }, { clientOptions: trustedConfig.clientOptions });
    const materializedTrustedManifest = resolvedTrustedManifest
        ? await (0, mountSecurity_1.resolveAndValidateMountEnvironment)(resolvedTrustedManifest)
        : undefined;
    const state = await client.deserializeSessionState(providerStateWithSdkEnvelopeFields(envelope, prevalidatedPersistedState.manifest, materializedTrustedManifest));
    if (client.backendId === 'docker') {
        delete state.sessionIdentity;
    }
    const credentialReboundState = (0, manifestPersistence_1.rebindPersistedMountCredentials)({
        ...state,
        ...(0, manifestPersistence_1.deserializeHostPathGrantRedactionMetadata)(envelope.providerState),
        ...(0, manifestPersistence_1.deserializeMountCredentialRedactionMetadata)(envelope.providerState),
    }, materializedTrustedManifest);
    const reboundState = (0, manifestPersistence_1.rebindPersistedPathGrants)(credentialReboundState, materializedTrustedManifest, {
        replaceWithTrustedManifest: client.backendId === 'docker' &&
            materializedTrustedManifest !== undefined,
    });
    (0, manifestPersistence_1.assertMountCredentialsRebound)(reboundState);
    (0, mountSecurity_1.validateMountCredentialBoundaries)(reboundState.manifest);
    (0, manifestPersistence_1.assertHostPathGrantsRebound)(reboundState);
    return (0, sessionStateTrust_1.markRunStateSessionState)(reboundState, trustedConfig);
}
function resolveTrustedManifestForResume(client, trustedManifest, clientOptions) {
    if (!trustedManifest) {
        return undefined;
    }
    const resolved = client.resolveTrustedManifestForResume
        ? client.resolveTrustedManifestForResume(trustedManifest, clientOptions)
        : trustedManifest;
    (0, mountSecurity_1.validateMountCredentialBoundaries)(resolved);
    return resolved;
}
function assertTrustedManifestForDockerRunState(client, trustedManifest) {
    if (client.backendId === 'docker' && trustedManifest === undefined) {
        throw new errors_1.UserError('Docker RunState resume requires a current trusted manifest for the sandbox agent.');
    }
}
function providerStateWithSdkEnvelopeFields(envelope, persistedManifest, trustedManifest) {
    const providerState = providerStateWithoutSdkEnvelopeFields(envelope.providerState, envelope.backendId);
    const persistedEnvironment = isStringRecord(providerState.environment)
        ? providerState.environment
        : undefined;
    const sanitizedEnvironment = persistedEnvironment
        ? (0, mountSecurity_1.sanitizeMountCredentialEnvironmentForPersistence)({
            manifest: trustedManifest ?? persistedManifest,
            environment: persistedEnvironment,
        }).environment
        : undefined;
    return {
        ...providerState,
        ...(sanitizedEnvironment ? { environment: sanitizedEnvironment } : {}),
        manifest: trustedManifest
            ? {
                ...(0, manifestPersistence_1.serializeManifestRecord)(persistedManifest),
                environment: cloneManifestEnvironment(trustedManifest.environment),
            }
            : (0, manifestPersistence_1.serializeManifestRecord)(persistedManifest),
        ...(envelope.snapshot !== undefined ? { snapshot: envelope.snapshot } : {}),
        ...(envelope.snapshotFingerprint !== undefined
            ? { snapshotFingerprint: envelope.snapshotFingerprint }
            : {}),
        ...(envelope.snapshotFingerprintVersion !== undefined
            ? {
                snapshotFingerprintVersion: envelope.snapshotFingerprintVersion,
            }
            : {}),
        workspaceReady: envelope.workspaceReady,
        ...(envelope.exposedPorts
            ? { exposedPorts: structuredClone(envelope.exposedPorts) }
            : {}),
    };
}
function isStringRecord(value) {
    return (typeof value === 'object' &&
        value !== null &&
        !Array.isArray(value) &&
        Object.values(value).every((entry) => typeof entry === 'string'));
}
function assertTrustedEnvironmentForPersistedReferences(envelope, trustedManifest) {
    const persistedEnvironment = envelope.manifest.environment;
    if (!persistedEnvironment ||
        typeof persistedEnvironment !== 'object' ||
        Array.isArray(persistedEnvironment)) {
        return;
    }
    const referenceKeys = Object.entries(persistedEnvironment)
        .filter(([, value]) => (0, manifest_1.isSerializedEnvValueReference)(value))
        .map(([key]) => key);
    if (referenceKeys.length === 0) {
        return;
    }
    if (!trustedManifest) {
        throw new errors_1.UserError('RunState sandbox session state with environment value references requires a current trusted manifest for the sandbox agent.');
    }
    const missingReferenceKeys = referenceKeys.filter((key) => !trustedManifest.environment[key] ||
        !(0, manifest_1.isEnvValueReference)(trustedManifest.environment[key]));
    if (missingReferenceKeys.length > 0) {
        throw new errors_1.UserError(`RunState sandbox session state contains environment value references that are not present in the current trusted manifest: ${missingReferenceKeys.join(', ')}.`);
    }
}
function cloneManifestEnvironment(environment) {
    return Object.fromEntries(Object.entries(environment).map(([key, value]) => [key, value.init()]));
}
function getSerializedSessionEntryForAgent(sandboxState, currentAgentKey) {
    if (!sandboxState) {
        return undefined;
    }
    return (sandboxState.sessionsByAgent?.[currentAgentKey] ??
        (sandboxState.currentAgentKey === currentAgentKey
            ? {
                backendId: sandboxState.backendId,
                currentAgentKey: sandboxState.currentAgentKey,
                currentAgentName: sandboxState.currentAgentName,
                sessionState: sandboxState.sessionState,
            }
            : undefined));
}
function serializedSessionEntryHasInContainerMounts(entry) {
    return entry
        ? (0, mountSecurity_1.manifestHasInContainerMounts)((0, manifestPersistence_1.deserializeManifest)(entry.sessionState.manifest))
        : false;
}
function serializedSessionEntryRequiresFreshMountAuthority(entry) {
    return (serializedSessionEntryHasInContainerMounts(entry) ||
        entry?.sessionState.providerState[mountSecurity_1.NON_RESUMABLE_MOUNT_AUTHORITY_KEY] ===
            true);
}
function getPreviousSerializedSessionsByAgent(sandboxState, client) {
    if (!sandboxState || sandboxState.backendId !== client.backendId) {
        return {};
    }
    const entries = {
        ...(sandboxState.sessionsByAgent ?? {}),
    };
    if (sandboxState.currentAgentKey && !entries[sandboxState.currentAgentKey]) {
        entries[sandboxState.currentAgentKey] = {
            backendId: sandboxState.backendId,
            currentAgentKey: sandboxState.currentAgentKey,
            currentAgentName: sandboxState.currentAgentName,
            sessionState: sandboxState.sessionState,
        };
    }
    return Object.fromEntries(Object.entries(entries).filter(([, entry]) => entry.backendId === client.backendId));
}
function hasPreservedOwnedSessions(sandboxState) {
    return Object.values(sandboxState?.sessionsByAgent ?? {}).some((entry) => entry.preservedOwnedSession);
}
//# sourceMappingURL=sessionState.js.map