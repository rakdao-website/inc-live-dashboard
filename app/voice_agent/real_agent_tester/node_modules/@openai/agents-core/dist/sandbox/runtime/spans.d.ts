import { Trace } from '../../tracing';
import type { Span } from '../../tracing';
import type { CustomSpanData } from '../../tracing/spans';
export declare function withSandboxSpan<T>(name: string, data: Record<string, unknown>, fn: (span?: Span<CustomSpanData>) => Promise<T>, parent?: Span<any> | Trace): Promise<T>;
