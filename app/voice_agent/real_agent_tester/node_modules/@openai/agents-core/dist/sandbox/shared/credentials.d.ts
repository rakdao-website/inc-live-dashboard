import { type SandboxErrorCode } from '../errors';
export type CredentialPairValidation = {
    accessKeyId?: string;
    secretAccessKey?: string;
    message: string;
    details?: Record<string, unknown>;
    code?: SandboxErrorCode;
};
export declare function validateCredentialPair({ accessKeyId, secretAccessKey, message, details, code, }: CredentialPairValidation): void;
