import { SandboxMountError } from "../errors.mjs";
export function validateCredentialPair({ accessKeyId, secretAccessKey, message, details, code, }) {
    if (Boolean(accessKeyId) !== Boolean(secretAccessKey)) {
        throw new SandboxMountError(message, details, code);
    }
}
//# sourceMappingURL=credentials.mjs.map