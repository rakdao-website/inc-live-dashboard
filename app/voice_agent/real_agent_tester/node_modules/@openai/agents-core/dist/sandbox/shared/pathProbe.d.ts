export type SandboxPathProbeResult = {
    status: number | null;
    stdout?: string;
    stderr?: string;
    signal?: string | null;
    timedOut?: boolean;
    error?: Error;
};
type SandboxPathProbeOptions = {
    path: string;
    runCommand: (command: string) => Promise<SandboxPathProbeResult>;
    createError?: (result: SandboxPathProbeResult) => Error;
};
export declare function isSandboxPathNotFoundError(error: unknown): boolean;
export declare function probeSandboxPathExists(options: SandboxPathProbeOptions): Promise<boolean>;
export declare function probeSandboxDirectoryExists(options: SandboxPathProbeOptions): Promise<boolean>;
export {};
