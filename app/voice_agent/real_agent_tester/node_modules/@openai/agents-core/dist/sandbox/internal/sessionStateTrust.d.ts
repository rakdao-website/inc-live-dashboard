import type { SandboxClientOptions } from '../client';
import type { SandboxSessionState } from '../session';
import type { SnapshotSpec } from '../snapshot';
export type RunStateSessionTrustedConfig = {
    clientOptions?: SandboxClientOptions;
    snapshot?: SnapshotSpec;
};
export declare function markRunStateSessionState<TState extends SandboxSessionState>(state: TState, trustedConfig?: RunStateSessionTrustedConfig): TState;
export declare function isRunStateSessionState(state: SandboxSessionState): boolean;
export declare function getRunStateSessionTrustedConfig(state: SandboxSessionState): RunStateSessionTrustedConfig | undefined;
