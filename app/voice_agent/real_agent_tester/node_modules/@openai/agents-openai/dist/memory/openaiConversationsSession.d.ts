import type { AgentInputItem, Session } from '@openai/agents-core';
import { OPENAI_SESSION_API, type OpenAISessionApiTagged } from './openaiSessionApi';
import type { OpenAIClient } from '../openaiClient';
export type OpenAIConversationsSessionOptions = {
    conversationId?: string;
    client?: OpenAIClient;
    apiKey?: string;
    baseURL?: string;
    organization?: string;
    project?: string;
};
export declare function startOpenAIConversationsSession(client?: OpenAIClient): Promise<string>;
export declare class OpenAIConversationsSession implements Session, OpenAISessionApiTagged<'conversations'> {
    #private;
    readonly [OPENAI_SESSION_API]: "conversations";
    constructor(options?: OpenAIConversationsSessionOptions);
    get sessionId(): string | undefined;
    getSessionId(): Promise<string>;
    getItems(limit?: number): Promise<AgentInputItem[]>;
    prepareHistoryItemForModelInput(item: AgentInputItem): AgentInputItem;
    prepareHistoryItemsForPersistenceComparison(items: AgentInputItem[]): AgentInputItem[];
    preserveReasoningItemIdsForPersistence(): boolean;
    addItems(items: AgentInputItem[]): Promise<void>;
    replaceHistoryWithCompaction(items: AgentInputItem[]): Promise<void>;
    popItem(): Promise<AgentInputItem | undefined>;
    clearSession(): Promise<void>;
}
