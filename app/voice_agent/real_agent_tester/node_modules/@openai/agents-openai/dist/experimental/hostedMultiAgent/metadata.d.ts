export type HostedAgentMetadata = Readonly<{
    agentName: string;
    phase?: string;
}>;
/**
 * Reads hosted-agent attribution without affecting local tool routing.
 */
export declare function getHostedAgentMetadata(value: unknown): HostedAgentMetadata | undefined;
