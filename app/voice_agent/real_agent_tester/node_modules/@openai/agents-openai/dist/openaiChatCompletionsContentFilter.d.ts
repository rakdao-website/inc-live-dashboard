import type { ChatCompletion } from 'openai/resources/chat';
export declare const CONTENT_FILTER_REFUSAL_MESSAGE = "Response withheld by the provider's content filter.";
type ContentFilterState = {
    finishReason: ChatCompletion['choices'][number]['finish_reason'] | null | undefined;
    hasOutput: boolean;
};
export declare function shouldSynthesizeContentFilterRefusal({ finishReason, hasOutput, }: ContentFilterState): boolean;
export {};
