import type { RealtimeClientMessage, RealtimeSessionConfig, RealtimeUserInput } from '../clientMessages';
import type { RealtimeItem, RealtimeMcpCallApprovalRequestItem } from '../items';
import type { RealtimeTransportLayer, RealtimeTransportLayerConnectOptions } from '../transportLayer';
import type { RealtimeTransportEventTypes, TransportToolCallEvent } from '../transportLayerEvents';
type FunctionKeys<T> = {
    [TKey in keyof T]-?: NonNullable<T[TKey]> extends (...args: any[]) => any ? TKey : never;
}[keyof T];
export type RealtimeTransportMethod = Exclude<FunctionKeys<RealtimeTransportLayer>, 'emit' | 'off' | 'on' | 'once'>;
type DeclaredRealtimeTransportCallData = {
    connect: {
        options: Omit<RealtimeTransportLayerConnectOptions, 'apiKey'> & {
            apiKeyProvided: boolean;
        };
    };
    sendEvent: {
        event: RealtimeClientMessage;
    };
    requestResponse: {
        response?: Record<string, any>;
    };
    sendMessage: {
        message: RealtimeUserInput;
        otherEventData: Record<string, any>;
        options?: {
            triggerResponse?: boolean;
        };
    };
    addImage: {
        image: string;
        options?: {
            triggerResponse?: boolean;
        };
    };
    sendAudio: {
        audio: ArrayBuffer;
        options: {
            commit?: boolean;
        };
    };
    updateSessionConfig: {
        config: Partial<RealtimeSessionConfig>;
    };
    close: Record<never, never>;
    mute: {
        muted: boolean;
    };
    sendFunctionCallOutput: {
        toolCall: TransportToolCallEvent;
        output: string;
        startResponse: boolean;
    };
    interrupt: Record<never, never>;
    resetHistory: {
        oldHistory: RealtimeItem[];
        newHistory: RealtimeItem[];
    };
    sendMcpResponse: {
        approvalRequest: RealtimeMcpCallApprovalRequestItem;
        approved: boolean;
        reason?: string;
    };
};
type ExactCallDataMap<TData> = TData extends Record<RealtimeTransportMethod, object> ? Exclude<keyof TData, RealtimeTransportMethod> extends never ? TData : never : never;
type RealtimeTransportCallData = ExactCallDataMap<DeclaredRealtimeTransportCallData>;
/** A forward-compatible summary of an outbound transport call. */
export type RecordedRealtimeTransportCall = Readonly<{
    /** The zero-based position of this call in `calls`. */
    index: number;
    /** The invoked method. New SDK versions may add method names. */
    method: string;
}>;
export type RealtimeTransportCallFor<TMethod extends RealtimeTransportMethod> = Readonly<{
    index: number;
    method: TMethod;
} & RealtimeTransportCallData[TMethod]>;
export type RealtimeCallMatcher<TMethod extends RealtimeTransportMethod> = (call: RealtimeTransportCallFor<TMethod>) => boolean | void;
export type ScriptedRealtimeScenarioContext = {
    expectCall: <TMethod extends RealtimeTransportMethod>(method: TMethod, matcher?: RealtimeCallMatcher<TMethod>) => Promise<RealtimeTransportCallFor<TMethod>>;
    emit: <TEvent extends keyof RealtimeTransportEventTypes>(event: TEvent, ...args: RealtimeTransportEventTypes[TEvent]) => boolean;
};
/** The callbacks and cancellation signal owned by `runScenario()`. */
export type ScriptedRealtimeScenarioOptions<TResult = void> = {
    scenario: (context: ScriptedRealtimeScenarioContext) => void | Promise<void>;
    exercise: () => TResult | Promise<TResult>;
    signal?: AbortSignal;
};
export type PendingRealtimeExpectation = Readonly<{
    order: number;
    method: RealtimeTransportMethod;
}>;
export type PendingRealtimeFailure = Readonly<{
    method: RealtimeTransportMethod;
    count: number;
}>;
export declare class UnexpectedRealtimeCallError extends Error {
    /** The zero-based position of the call in `ScriptedRealtimeTransport.calls`. */
    readonly callIndex: number;
    readonly expectedMethod: RealtimeTransportMethod;
    readonly actualMethod: string;
    constructor(expectedMethod: RealtimeTransportMethod, actualCall: RecordedRealtimeTransportCall);
}
export declare class RealtimeCallMatcherError extends Error {
    /** The zero-based position of the call in `ScriptedRealtimeTransport.calls`. */
    readonly callIndex: number;
    readonly actualMethod: string;
    constructor(call: RecordedRealtimeTransportCall);
}
export declare class IncompleteRealtimeScenarioError extends Error {
    readonly unconsumedCalls: readonly RecordedRealtimeTransportCall[];
    readonly pendingExpectations: readonly PendingRealtimeExpectation[];
    readonly pendingFailures: readonly PendingRealtimeFailure[];
    constructor(unconsumedCalls: readonly RecordedRealtimeTransportCall[], pendingExpectations: readonly PendingRealtimeExpectation[], pendingFailures: readonly PendingRealtimeFailure[]);
}
export declare class ScriptedRealtimeScenarioCancelledError extends Error {
    readonly pendingExpectations: readonly PendingRealtimeExpectation[];
    constructor(pendingExpectations: readonly PendingRealtimeExpectation[]);
}
export declare class RealtimeTransportNotClosedError extends Error {
    readonly status: ScriptedRealtimeTransport['status'];
    constructor(status: ScriptedRealtimeTransport['status']);
}
export declare class ScriptedRealtimeConnectionSupersededError extends Error {
    readonly operation: "connect";
    constructor();
}
/**
 * An in-memory Realtime transport for deterministic session tests.
 */
export declare class ScriptedRealtimeTransport implements RealtimeTransportLayer {
    #private;
    status: 'connected' | 'disconnected' | 'connecting' | 'disconnecting';
    muted: boolean | null;
    /** Registers a synchronous transport event listener. */
    on<TEvent extends keyof RealtimeTransportEventTypes>(event: TEvent, listener: (...args: RealtimeTransportEventTypes[TEvent]) => void): this;
    /** Removes the most recently registered matching listener. */
    off<TEvent extends keyof RealtimeTransportEventTypes>(event: TEvent, listener: (...args: RealtimeTransportEventTypes[TEvent]) => void): this;
    /** Emits an event synchronously and propagates listener failures. */
    emit<TEvent extends keyof RealtimeTransportEventTypes>(event: TEvent, ...args: RealtimeTransportEventTypes[TEvent]): boolean;
    /** Registers a synchronous one-time transport event listener. */
    once<TEvent extends keyof RealtimeTransportEventTypes>(event: TEvent, listener: (...args: RealtimeTransportEventTypes[TEvent]) => void): this;
    /** All outbound calls recorded in invocation order. */
    get calls(): readonly RecordedRealtimeTransportCall[];
    /** Registers an ordered expectation for the next outbound call. */
    expectCall<TMethod extends RealtimeTransportMethod>(method: TMethod, matcher?: RealtimeCallMatcher<TMethod>): Promise<RealtimeTransportCallFor<TMethod>>;
    /** Runs an exercised scenario without relying on a wall-clock timeout. */
    runScenario<TResult>(options: ScriptedRealtimeScenarioOptions<TResult>): Promise<TResult>;
    /** Makes the next call to a transport method throw the supplied error. */
    failNextCall(method: RealtimeTransportMethod, error: unknown): void;
    /** Emits a controlled terminal disconnect and optional transport error. */
    disconnect(error?: unknown): void;
    /** Throws when calls or expectations remain unmatched. */
    assertComplete(): void;
    /** Throws unless the transport is disconnected. */
    assertClosed(): void;
    connect(options: RealtimeTransportLayerConnectOptions): Promise<void>;
    sendEvent(event: RealtimeClientMessage): void;
    requestResponse(response?: Record<string, any>): void;
    sendMessage(message: RealtimeUserInput, otherEventData: Record<string, any>, options?: {
        triggerResponse?: boolean;
    }): void;
    addImage(image: string, options?: {
        triggerResponse?: boolean;
    }): void;
    sendAudio(audio: ArrayBuffer, options: {
        commit?: boolean;
    }): void;
    updateSessionConfig(config: Partial<RealtimeSessionConfig>): void;
    close(): void;
    mute(muted: boolean): void;
    sendFunctionCallOutput(toolCall: TransportToolCallEvent, output: string, startResponse: boolean): void;
    interrupt(): void;
    resetHistory(oldHistory: RealtimeItem[], newHistory: RealtimeItem[]): void;
    sendMcpResponse(approvalRequest: RealtimeMcpCallApprovalRequestItem, approved: boolean, reason?: string): void;
}
export {};
